{% extends 'base.html.twig' %}

{% block title %}Reaction index{% endblock %}

{% block body %}
    <h1>Reaction index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>CreatedAt</th>
                <th>UpdateAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reaction in reactions %}
            <tr>
                <td>{{ reaction.id }}</td>
                <td>{{ reaction.type }}</td>
                <td>{{ reaction.createdAt ? reaction.createdAt|date('Y-m-d') : '' }}</td>
                <td>{{ reaction.updateAt ? reaction.updateAt|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('reaction_show', {'id': reaction.id}) }}">show</a>
                    <a href="{{ path('reaction_edit', {'id': reaction.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('reaction_new') }}">Create new</a>
{% endblock %}
